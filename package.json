{
  "name": "fastread",
  "version": "1.0.0",
  "description": "Fast Reading",
  "main": "index.js",
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rollup -c",
    "build-css": "node-sass ./src/style.scss --output dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shunsuke Ito",
  "license": "ISC",
  "dependencies": {
    "vanilla-framework": "^2.0.0-beta.1"
  },
  "devDependencies": {
    "node-sass": "^8.0.0"
  }
}
